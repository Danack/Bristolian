I R WEBSOCKETS SERVAL.

You probably want to bash into the box to start the server.

Automatic start was disabled for dev, so the chat servercan be restarted very quickly.

Do something like:

cd /var/app/chat/src
exec php index.php

